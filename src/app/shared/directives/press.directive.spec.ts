import { PressDirective } from './press.directive';

describe('LongPressDirective', () => {
  it('should create an instance', () => {
    const directive = new PressDirective(null);
    expect(directive).toBeTruthy();
  });
});
